The backend at version 1.2.0 just shipped. Post a brief, friendly release-note
message to the #releases channel summarizing what the backend contains: FastAPI
app, SQLModel-based user + item resources, JWT login, and Alembic migrations.
Keep it under 4 sentences. Use the Slack plugin tools available to you to
actually post — discover the releases channel via slack_search_channels first,
then use slack_send_message with that channel_id.
